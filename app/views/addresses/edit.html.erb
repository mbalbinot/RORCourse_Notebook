<div class="form-style-6">

  <h1><%= t('messages.editing') %> <%= @address.model_name.human %></h1>

  <%= render 'form' %>

  <%= link_to t('buttons.show'), @address %> |
  <%= link_to t('buttons.back'), addresses_path %>

</div>