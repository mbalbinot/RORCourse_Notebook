<div class="form-style-6">

  <h1><%= t('messages.editing') %> <%= @phone.model_name.human %></h1>

  <%= render 'form' %>

    <%= link_to t('buttons.show'), @phone %> |
    <%= link_to t('buttons.back'), phones_path %>

</div>